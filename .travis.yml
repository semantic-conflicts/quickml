
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn clean compile package assembly:single -DskipTests

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/semantic-conflicts/quickml/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-ecd8c80d7263bce611acdeee2aad994736b04fbc
  file_glob: true
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true 
            